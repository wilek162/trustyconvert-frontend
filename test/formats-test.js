/**
 * Test script to verify the formats integration
 * This script can be run manually to test the formats fetching functionality
 */

// Import the fetch-formats script directly to test it
import '../src/scripts/fetch-formats.js';

console.log('Test script completed. Check the public/data directory for the formats.json file.'); 