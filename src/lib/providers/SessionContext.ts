/**
 * SessionContext re-export
 * 
 * This file re-exports the SessionProvider and useSession from the components/providers directory
 * to maintain backward compatibility with existing imports.
 */

export { useSession } from '@/components/providers/SessionProvider' 