/**
 * Error handling utilities
 * 
 * Re-exports error handling functions for convenient use in components
 */

import { withErrorHandling, handleError } from '@/lib/errors/errorHandlingService'

export { withErrorHandling, handleError } 